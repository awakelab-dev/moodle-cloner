.env

# Local infrastructure config (contains SSH/sudo passwords in plaintext).
# Use inventario.example.json as the template for new environments.
inventario.json

# Alexia cloner config (contains SSH/sudo passwords in plaintext).
alexia_config.json
temp_alexia/
logs

# Python cache/artifacts
__pycache__/
*.py[cod]

# macOS
.DS_Store
package-lock.json

# Carpeta de descarte: las sesiones de Cowork no pueden borrar archivos en
# carpetas montadas, asi que mueven ahi lo que hay que eliminar a mano.
_to_delete/
